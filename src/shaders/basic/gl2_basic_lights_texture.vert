#version 300 es
precision mediump float;uniform mat4 MVMat;uniform mat4 PMat;in vec3 VPos;in vec2 uv;out vec2 fragUV;out vec3 fragVPos;void main(){vec4 VPos4 = MVMat*vec4(VPos, 1.0);gl_Position = PMat*VPos4;fragVPos = vec3(VPos4)/VPos4.w;fragUV = uv;}